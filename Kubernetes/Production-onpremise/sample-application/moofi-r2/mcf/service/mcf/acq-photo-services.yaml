apiVersion: v1
kind: Service
metadata:
  name: acq-photo-services
  #namespace: development
spec:
  ports:
  - port: 5098
    protocol: TCP
    targetPort: 80
  selector:
    app: acq-photo-services
  type: ClusterIP
