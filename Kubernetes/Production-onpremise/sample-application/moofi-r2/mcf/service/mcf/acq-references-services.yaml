apiVersion: v1
kind: Service
metadata:
  name: acq-references-services
  #namespace: development
spec:
  ports:
  - port: 5092
    protocol: TCP
    targetPort: 80
  selector:
    app: acq-references-services
  type: ClusterIP
