apiVersion: v1
kind: Service
metadata:
  name: acq-guarantor-services
  #namespace: development
spec:
  ports:
  - port: 5093
    protocol: TCP
    targetPort: 80
  selector:
    app: acq-guarantor-services
  type: ClusterIP
