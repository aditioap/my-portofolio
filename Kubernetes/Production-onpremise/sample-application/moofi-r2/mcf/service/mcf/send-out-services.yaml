apiVersion: v1
kind: Service
metadata:
  name: send-out-services
  #namespace: development
spec:
  ports:
  - port: 5112
    protocol: TCP
    targetPort: 3344
  selector:
    app: send-out-services
  type: ClusterIP
