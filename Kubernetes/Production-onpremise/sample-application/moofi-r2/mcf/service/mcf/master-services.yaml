apiVersion: v1
kind: Service
metadata:
  name: master-services
  #namespace: development
spec:
  ports:
  - port: 5087
    protocol: TCP
    targetPort: 80
  selector:
    app: mongo-services
  type: ClusterIP
