apiVersion: v1
kind: Service
metadata:
  name: login-services
  #namespace: development
spec:
  ports:
  - port: 5101
    protocol: TCP
    targetPort: 80
  selector:
    app: login-services
  type: ClusterIP
