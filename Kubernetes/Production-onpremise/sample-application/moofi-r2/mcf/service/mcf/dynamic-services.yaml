apiVersion: v1
kind: Service
metadata:
  name: dynamic-services
  #namespace: development
spec:
  ports:
  - port: 5085
    protocol: TCP
    targetPort: 80
  selector:
    app: dynamic-services
  type: ClusterIP
