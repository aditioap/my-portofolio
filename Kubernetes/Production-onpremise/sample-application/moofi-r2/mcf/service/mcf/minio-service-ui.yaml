apiVersion: v1
kind: Service
metadata:
  name: minio-service-ui
  #namespace: development
spec:
  ports:
  - port: 9001
    protocol: TCP
    targetPort: 9001
  selector:
    app: minio
  type: ClusterIP
