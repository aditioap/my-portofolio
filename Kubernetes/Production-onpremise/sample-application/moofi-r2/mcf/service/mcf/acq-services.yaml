apiVersion: v1
kind: Service
metadata:
  name: acq-services
  #namespace: development
spec:
  ports:
  - port: 5086
    protocol: TCP
    targetPort: 80
  selector:
    app: acq-services
  type: ClusterIP
