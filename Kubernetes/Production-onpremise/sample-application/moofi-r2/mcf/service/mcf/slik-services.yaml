apiVersion: v1
kind: Service
metadata:
  name: slik-services
  #namespace: development
spec:
  ports:
  - port: 5114
    protocol: TCP
    targetPort: 80
  selector:
    app: slik-services
  type: ClusterIP
