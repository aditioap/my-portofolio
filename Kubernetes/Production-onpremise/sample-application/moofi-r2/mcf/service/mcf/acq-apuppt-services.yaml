apiVersion: v1
kind: Service
metadata:
  name: acq-apuppt-services
  #namespace: development
spec:
  ports:
  - port: 5094
    protocol: TCP
    targetPort: 80
  selector:
    app: acq-apuppt-services
  type: ClusterIP
