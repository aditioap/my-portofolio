apiVersion: v1
kind: Service
metadata:
  name: biometric-services
  namespace: uat-moofi-mcf
spec:
  ports:
  - port: 80 ## --> Port Service 
    protocol: TCP
    targetPort: 8080 ## --> Port Container/Pod
  selector:
    app: biometric-services
  type: ClusterIP
