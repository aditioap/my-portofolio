apiVersion: v1
kind: Service
metadata:
  name: ocr-app-service
  #namespace: development
spec:
  ports:
  - port: 3637
    protocol: TCP
    targetPort: 3637
  selector:
    app: ocr-app
  type: ClusterIP
