apiVersion: v1
kind: Service
metadata:
  name: minio-services
  #namespace: development
spec:
  ports:
  - port: 5095
    protocol: TCP
    targetPort: 80
  selector:
    app: minio-services
  type: ClusterIP
