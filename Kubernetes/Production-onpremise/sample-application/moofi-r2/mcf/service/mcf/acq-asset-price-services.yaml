apiVersion: v1
kind: Service
metadata:
  name: acq-asset-price-services
  #namespace: development
spec:
  ports:
  - port: 5089
    protocol: TCP
    targetPort: 80
  selector:
    app: acq-asset-price-services
  type: ClusterIP
