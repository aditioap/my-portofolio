apiVersion: v1
kind: Service
metadata:
  name: veriface-services
  #namespace: development
spec:
  ports:
  - port: 5115
    protocol: TCP
    targetPort: 5115
  selector:
    app: veriface-services
  type: ClusterIP
