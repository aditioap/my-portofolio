apiVersion: v1
kind: Service
metadata:
  name: dealer-list-services
  #namespace: development
spec:
  ports:
  - port: 5110
    protocol: TCP
    targetPort: 80
  selector:
    app: dealer-list-services
  type: ClusterIP
