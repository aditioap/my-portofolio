apiVersion: v1
kind: Service
metadata:
  name: fin-slikojk-services
  #namespace: uat-mcf
spec:
  ports:
    - port: 80
      protocol: TCP
      targetPort: 80
  selector:
    app: fin-slikojk-services
  type: ClusterIP
