apiVersion: v1
kind: Service
metadata:
  name: fin-cm-services
  namespace: uat-mcf
spec:
  ports:
    - port: 80
      protocol: TCP
      targetPort: 80
  selector:
    app: fin-cm-services
  type: ClusterIP
