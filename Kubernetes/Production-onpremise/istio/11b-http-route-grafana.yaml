apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: grafana
  namespace: istio-system
spec:
  parentRefs:
  - name: uat-gateway
    namespace: istio-system
  hostnames:
  - "grafana.macf.co.id"
  rules:
  - backendRefs:
    - name: grafana
      port: 3000
