apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: konga-dashboard
  namespace: kong
spec:
  parentRefs:
  - name: uat-gateway
    namespace: istio-system
  hostnames:
  - "konga.macf.co.id"
  rules:
  - backendRefs:
    - name: konga
      port: 1337
