apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: rabbitmq-dashboard
  namespace: rabbitmq-system
spec:
  parentRefs:
  - name: uat-gateway
    namespace: istio-system
  hostnames:
  - "rabbit-prod.macf.co.id"
  rules:
  - backendRefs:
    - name: rabbitmq-prod
      port: 15672
