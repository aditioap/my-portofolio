apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: prometheus
  namespace: istio-system
spec:
  parentRefs:
  - name: uat-gateway
    namespace: istio-system
  hostnames:
  - "prometheus.macf.co.id"
  rules:
  - backendRefs:
    - name: prometheus 
      port: 9090
