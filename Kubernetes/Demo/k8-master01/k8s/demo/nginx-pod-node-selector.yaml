apiVersion: v1
kind: Pod
metadata:
  name: nginx-node-selector-tes1
spec:
  nodeSelector:
    ssd: "true"
  containers:
    - name: nginx-node-selector-tes1
      image: nginx
      ports:
        - containerPort: 80
